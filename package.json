{
  "name": "jedavid-web-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "audit": "pnpm --filter @jedavid/web-tools-cli dev --",
    "mcp": "pnpm --filter @jedavid/web-tools-mcp dev"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.22.5",
    "typescript": "^7.0.2"
  }
}
