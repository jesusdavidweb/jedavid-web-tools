{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "jedavid-web-tools": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/bin/jedavid-web-tools-mcp.mjs"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
